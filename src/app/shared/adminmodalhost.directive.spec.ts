import { AdminmodalhostDirective } from './adminmodalhost.directive';

describe('AdminmodalhostDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminmodalhostDirective();
    expect(directive).toBeTruthy();
  });
});
